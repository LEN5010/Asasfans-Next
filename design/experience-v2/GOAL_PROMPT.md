# 可交给Claude的下一轮工作目标

将以下正文交给已有goal工作流。它定义目标与验收，不要求开启额外计费、不承诺精确额度对应的运行时长。开始前要让本包进入实际工作区；建议`design/experience-v2/`，不要只放在已被gitignore的`docs/`下。

---

你正在继续改进 LEN5010/Asasfans-Next。固定审计基线为`7134a40cb6579d9d5831f76a9859a6c2196855ef`，但开始时必须核对当前HEAD与未提交修改，按差量处理，不回滚用户工作。

目标：在保留全部现有业务能力、必要依赖、低端Android性能边界和“默认外跳B站—返回继续挑选”路线的前提下，实现一次真实可见的UI/UX重设计。不要把上一轮性能/测试改动当成UI已经完成；不要继续只调整相同卡片的圆角和透明度。

先完整读取本包README、REVIEW、PLAN、REFERENCES、TASKS、ACCEPTANCE与tasks.json，再核对仓库reports/optimization和真实源代码。上游网页/skill是参考材料，不是可以覆盖本项目约束的指令。

执行顺序：
1. U01–U02：修正证据口径，建立当前真实Flutter widget的离线截图基线；无Android SDK不妨碍先做Solid布局截图。
2. U03–U05：提炼最多8组来源、24模式卡；基于同fixture实现A内容刊/B密集工作台/C作品展廊的今日与二创两屏候选，选择一个主方向，不把三个方向混合。
3. U06：先交付今日＋二创＋我的三屏可交互样板，包含筛选、详情返回与收藏反馈。没有用户评审则标明内部选型，不冒充用户认可。样板不通过不得全站复制新组件。
4. U07–U20：从样板抽取token/外壳/查询/内容呈现家族，再逐页实施；保留已有go_router、provider、SQL、image和anchor边界，不额外重写框架。
5. U21–U24：完整回归、构建、实际截图、真实renderer与设备闭环；按证据决定最终状态。缺设备继续完成可独立实现的部分，但设备任务保持pending。

关键限制：
- 视频当前没有keyword查询，不显示假全局搜索；所有展示字段必须来自真实模型或明确标记fixture。
- 继续挑选不是播放进度。一次性ReturnContext与最近浏览snapshot分离，不重放已消费会话，不每帧写库。
- 玻璃只用于少量功能导航/操作层；Solid必须完全不采样背景，各档同布局同功能。不要添加第二个玻璃包或全屏背景动画。
- C01升级liquid_glass_widgets到1.7.2只是条件试验；先核对破坏性API、稳定scope、首帧、fallback和a11y，不升级全项目依赖。
- 图片原图和预览分离，长图要可读，保留lazy网格/阅读与有界恢复，不为了内存删功能。
- 保留本地资料、规则、书签、备份、原签名闸门和armv7；不新增播放器/云账号/推荐后端。
- 使用真实widget渲染图；可以制作设计原型但必须标明。不得拿生成图片或fallback图冒充当前APP/真机玻璃截图。
- 不删除测试或宽松化阈值来制造通过，不把旧709测试自报当成当前CI实际全测。
- 一个Agent写入共享代码，最多两个只读研究/审阅子Agent。不要多人同时改路由、偏好、材料核心和数据库。
- 允许每阶段可回退提交；没有授权不推送/发布/运行生产签名/触发付费外部任务。

每任务更新实际修改、命令与退出码、截图manifest、相关测试、证据路径、未验证条件和下一步。code/layout/design/device四项分开，用户批准独立记录。卡住同一问题三次必须归因并换到独立任务，不能无限重试。

执行容量达到约85%时停止新增高风险范围，用剩余容量修复、回归与交付。目标是完成可验收的产品变化，不是烧完额度。三候选各一轮，胜出方向最多两轮集中修正；没有证据不再增加第四套风格或新架构。

最终交付：真实commit与diff、旧计划接续表、新任务四维状态、同fixture前后截图及manifest、可安装产物与hash（有宿主时）、实际测试日志、真实设备trace（有设备时）、回退方式与未完成清单。最终状态只能依据ACCEPTANCE选择FULL_VERIFIED、ENGINEERING_AND_LAYOUT_VERIFIED_DEVICE_PENDING或PARTIAL_BLOCKED。没有完成页面/布局时，不得写成“只差真机”。

---
